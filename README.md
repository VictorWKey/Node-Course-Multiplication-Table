# Notes:

This is my first program in Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Is the base of the multiplication table     [number] [required]
  -l, --list     Show the multiplication table on console
                                                      [boolean] [default: false]
  -u, --until    how many multiplications do you want?       [number] [required]
```